// this is your configuration file
// export const CONFIG = {
// backend: {
// host: "localhost",
// port: "8080"
// }
// };
export const CONFIG = {
  backend: {
    host: "10.42.0.1",
    port: "8080"
  }
};
